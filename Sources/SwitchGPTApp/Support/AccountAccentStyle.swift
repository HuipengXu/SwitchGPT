import SwiftUI
import SwitchGPTAppCore

extension AccountAccent {
  var color: Color {
    // Account accents remain part of the data model, but the product surface is monochrome.
    return .primary
  }
}
